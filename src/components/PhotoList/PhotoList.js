import React, { Component } from 'react';
import propTypes from 'prop-types';

export default class PhotoList extends Component {
    let photosURLs = this.props.photos.map
    render() {
        return
    }
}